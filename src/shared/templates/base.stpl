<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" /> 
    <% for path in css_paths { %>
      <link rel="stylesheet" href="<%= path %>">
    <% } %>
  </head>
  <body>
    <% include!("header.stpl"); %> 
    <%- content %>
    <% include!("footer.stpl"); %> 
  </body>
</html>
